# v0.3 · 产品骨架阶段收口报告

> **创建**：2026-06-15
> **作者**：蕾姆酱 🩵
> **状态**：✅ v0.3 阶段完成（M-α/M-β/M-γ/M-δ 4 个里程碑全部跑通）
> **关系**：本文件是 v0.3 收口报告，**不是新路线图**。`v0.3-product-skeleton.md` 是路线图，已同步状态。

---

## 0. 文档定位

### 0.1 与 v0.3-product-skeleton.md 的关系

| 文档 | 角色 |
|------|------|
| `v0.3-product-skeleton.md` | 路线图（活文档）—— 状态同步为"收口"，v0.3 段标 ✅ |
| `v0.3-completion.md`（本文件）| 收口报告（快照）—— 4 个里程碑汇总 + 关键数据 + 遗留问题 |

**收口报告 = 阶段里程碑的完整快照**，未来回看 v0.3 怎么走过来的，只读本文件即可。**路线图持续更新**，**收口报告永不变**。

### 0.2 范围

**v0.3 阶段** = M-α + M-β + M-γ + M-δ 4 个里程碑的并集。

**不在范围**：
- v0.2 评测（见 `docs/eval-notes/v0.2-real-llm.md`）
- M-ε（M4 UI · 已确认延后）
- v0.4 计划（见 §7）

---

## 1. 4 个里程碑汇总

### 1.1 M-α · 骨架 0.1（S1 ProjectManager + S2 WorldTree）

**完成时间**：2026-06-15 上午
**Commit**：`cf559cb feat(s1,s2): 骨架 0.1`

**产出**：
- S1 ProjectManager: `create()` / `load()` / `list_projects()` + Project/LoadedProject dataclass
- S2 WorldTree: 内存中聚合 7 件 Pydantic 模型 + `to_dict/from_dict/from_project_dir/to_project_dir` + `add_node/find_node/rollback_to/summary`
- 7 件 Pydantic Schema: WorldTree/StyleCharter/GenreResonance/MainPlot/SubPlot/CharacterCard/SeedTable + TreeNode + 各 enum
- I/O 适配器: `adapters/io.py` YAML/JSON 读写（按 `03 §4` 约定）
- 工具: `utils/seed_demo.py` 从 v0.2 case 转 demo YAML
- 验收脚本: `tests/m1/test_skeleton.py` (5/5 通过)

**验收结果**：
```
✅ 验收 1: ProjectManager.create() — 7 件空 YAML
✅ 验收 2: ProjectManager.load() — Pydantic 校验通过
✅ 验收 3: WorldTree.from_dict() — 反序列化成功
✅ 验收 4: WorldTree.to_dict() — round-trip 一致
✅ 额外: rollback_to() — 硬 reset + 边界场景
```

**数据规模**（demo 装载后）：branches 1 / beats 5 / characters 4 / relationships 3 / threads 2 / seeds 3 / accept 4 / reject 4

---

### 1.2 M-β · 骨架 0.2（S4 ChapterGenerator 接 LLM）

**完成时间**：2026-06-15 下午
**Commit**：`f7e0471 feat(s4): 骨架 0.2`

**产出**：
- S4 ChapterGenerator: `generate_next()` + GenerationResult dataclass + 4 步内部（找 beat / 组 prompt / 调 LLM / 提取摘要 / 落章节 / 更新 WorldTree）
- LLM 客户端: `adapters/llm.py`（重构后 v0.3.0 已**完全独立**——不引用 lunaris）
- 三层 prompt 组装: `adapters/prompt.py`（base/dynamic/recent + 人物 + beat + 种子 ID 清单）
- 种子权重: `adapters/seed_weight.py`（4 维加权 + 双重缩放 + sigmoid）
- ChapterSummary Schema: `core/schemas/chapter.py` + SeedChanges 子模型
- 验收脚本: `tests/m2/test_chapter.py` (5/5 通过)

**验收结果**（多次跑，平均）：
```
✅ 验收 1: 加载 demo WorldTree (7 件 → WorldTree 实例化)
✅ 验收 2: LLM 客户端可调用 (ping "OK")
✅ 验收 3: 生成真实产品流 (chapter-NN, ~3000-4500 字, ~60-100s)
✅ 验收 4: 章节落盘 (~7-13 KB, 写到 projects/{id}/chapters/)
✅ 验收 5: WorldTree 更新 (branches +1, 新 node-chapter-NN, seed 状态 0 触动)
```

**质量信号**（chapter-21 头几行）：
> # 第 21 章
> 星期天早上六点，林远醒了。
> 窗帘缝里漏进来一条灰色的光...
> 昨晚他们把收音机又试了一遍...

散文式 ✓ 冷叙述 ✓ 留白 ✓ 主角名字稳定（林远/陈岚）✓ 种子"收音机"自然延续 ✓

---

### 1.3 M-γ · 骨架 0.3（S3 OnboardingFlow 5 步启动链路）

**完成时间**：2026-06-15 傍晚
**Commit**：`96baef7 feat(s3): 骨架 0.3`

**产出**：
- S3 OnboardingFlow: 5 步引导 orchestrator（1a 必选标签 / 1b 可选标签 / 2 引导文本 / 3 大纲确认 / 4 后台准备 / 5 进入剧情）
- 状态持久化: `.onboarding-state.json` 写到 `projects/{id}/`，支持 resume / restart
- Step 4 调 LLM 把 1a/1b/2/3 答案 → 7 件 YAML（含 Pydantic 校验 + branches 兜底）
- Step 5 复用 S4 生成 chapter-01
- CLI 交互工具: `cli/interactive.py` 4 个零依赖 helper（prompt/confirm/multi_select/single_select）
- CLI 接入: `realtime-novel new --project-id <id> [--restart]`
- 验收脚本: `tests/m3/test_onboarding.py` (4/4 通过)

**验收结果**：
```
✅ 验收 1: 创建新项目目录 (7 件空 YAML)
✅ 验收 2: 5 步状态 → 7 件 YAML (7 次 LLM + Pydantic 校验通过)
✅ 验收 3: 自动生成第 1 章 (3526 字, ≥ 2500)
✅ 验收 4: 状态持久化 + 重新加载一致
```

**耗时**：~5 min（7 件 LLM × ~30s + 1 章 LLM × ~85s）

---

### 1.4 M-δ · 骨架 0.4（S5 InterventionParser + RollbackManager）

**完成时间**：2026-06-15 晚上
**Commit**：`5b6782f feat(s5): 骨架 0.4`

**产出**：
- S5 InterventionParser: 解析用户输入 → 结构化 Intervention（4 种输入模式自动检测 + system_msg 注入）
- S5 RollbackManager: 落盘式硬 reset（删除 Node + 删 chapter 文件 + 重新写 7 件 YAML）
- ChapterGenerator 升级: 新增 `system_msg` 参数（干预透传到 LLM）
- CLI 接入: `cmd_rollback` 重写（强确认 ROLLBACK）+ `cmd_intervene` 新增
- 验收脚本: `tests/m4/test_intervention.py` (3/3 通过)

**验收结果**：
```
✅ 验收 1: InterventionParser 4 种输入（我期望/我/我希望/自由文本）
✅ 验收 2: RollbackManager 硬 reset 落盘（删 2 Node + 3 章节, 7 件 schema_version 一致）
✅ 验收 3: 回档后继续生成新章节（3746 字, 0 状态污染）
```

**关键 bug 修复**（M-δ 首次暴露）：
- `core/world_tree.py: to_dict()` 加 `mode="json"` —— Pydantic enum 序列化为字符串，兼容 YAML 写入

---

## 2. v0.3 总验收

### 2.1 验收脚本通过情况

| 验收 | 项数 | 通过 | 状态 |
|------|------|------|------|
| M-α (test_skeleton.py) | 5 | 5 | ✅ 5/5 |
| M-β (test_chapter.py) | 5 | 5 | ✅ 5/5 |
| M-γ (test_onboarding.py) | 4 | 4 | ✅ 4/4 |
| M-δ (test_intervention.py) | 3 | 3 | ✅ 3/3 |
| **合计** | **17** | **17** | **✅ 17/17** |

### 2.2 CLI 5 子命令

| 命令 | 实装阶段 | 状态 |
|------|---------|------|
| `realtime-novel new` | M-γ | ✅ 5 步引导 |
| `realtime-novel load` | M-α | ✅ 7 件规模展示 |
| `realtime-novel generate` | M-β | ✅ 端到端生成章节 |
| `realtime-novel rollback` | M-δ | ✅ 落盘硬 reset |
| `realtime-novel intervene` | M-δ | ✅ 导演/演员解析 |

### 2.3 包结构（5 子包）

```
realtime_novel/                   # 5 子包
├── core/              # 核心数据 (7 件 Schema + WorldTree + ProjectManager + exceptions)
├── services/          # 业务服务 (S1-S5 orchestrators)
├── adapters/          # 外部依赖 (LLM / Prompt / IO / 算法)
├── cli/               # 命令行 (argparse 零依赖)
└── utils/             # 工具 (seed_demo / version)
```

依赖：**3 个第三方**（pydantic + pyyaml + certifi）+ 2 个 stdlib（urllib + ssl）。**零重型依赖**（无 Click / pytest / loguru）。

---

## 3. 关键数据

### 3.1 章节生成质量（M-β 实测多次）

| 章节 | 字数 | 用时 | 文件大小 |
|------|------|------|----------|
| chapter-21 (1st) | 2670 | 55.5s | 7.7 KB |
| chapter-21 (重构后) | 4521 | 100.3s | 13.0 KB |
| chapter-22 (重构后) | 3907 | 84.9s | 11.2 KB |
| chapter-23 (M-β 收口) | 4106 | 69.2s | 11.6 KB |

**质量稳定**：散文式 + 冷叙述 + 留白 + 主角名字稳定（林远/陈岚从未改名）

### 3.2 LLM 配置

- **模型**：deepseek-v4-pro（lunaris config 转发后实际是 OpenAI 兼容协议）
- **endpoint**：`https://aigc.sankuai.com/v1/openai/native`
- **章节温度**：0.7
- **JSON 温度**：0.5
- **完全独立**（不引用 lunaris 任何代码）—— 按 `conventions.md §5` 强约束

### 3.3 工程代码量

| 类别 | 文件数 | 行数（含测试）|
|------|--------|--------------|
| 产品代码 | 18 | ~1700 |
| 测试验收 | 4 | ~800 |
| 工程规范 | 1 | ~330 |
| 文档 | 9 | ~3700 |

---

## 4. 已落地 vs 暂未落地的对比

### 4.1 已落地 ✅

| 类别 | 项 |
|------|---|
| **数据层** | 7 件 Pydantic Schema + 摘要 Schema + Project/WorldTree 内存模型 |
| **服务层** | S1 ProjectManager / S2 WorldTree / S3 OnboardingFlow / S4 ChapterGenerator / S5 InterventionParser + RollbackManager |
| **适配层** | LLM 客户端（独立）+ 3 层 prompt + 种子权重 + YAML/JSON I/O |
| **CLI** | 5 子命令（argparse 零依赖）|
| **测试** | 4 个里程碑验收脚本（17/17 通过）|
| **工程规范** | `.realtime-novel/conventions.md` 15 条强约束 + `.tmp/` 隐藏目录不入仓 |
| **配置** | 独立 `config.private.json`（不入仓），环境变量 LLM_CONFIG_PATH 可覆盖 |
| **异常层级** | RealtimeNovelError 基类 + 5 类子异常 |
| **版本管理** | `__version__ = "0.3.0-alpha"`（semver）|
| **变更日志** | CHANGELOG.md（Keep a Changelog 风格）|

### 4.2 暂未落地 ❌（v0.4+ 再做）

| 类别 | 项 | 备注 |
|------|------|------|
| **算法层** | 4 维权重调优 / sigmoid k 调整 | 路线图 M1（v0.4）|
| **算法层** | overdue 命中率 | 路线图 M1（v0.4）|
| **架构** | 300+ 章远期挑战 | 路线图 M3（v0.4+）|
| **UI** | M4 启动链路 + 章节阅读器 + 干预输入 | 路线图 M-ε / M4（v0.4+）|
| **测试** | pytest 升级 + conftest.py + fixtures | v0.4 计划 |
| **协作** | 多 Agent 协作 | 路线图 M4（v0.4+）|
| **种子迭代** | LLM-as-judge 自查 | 已设计，待 v0.4 实装 |

---

## 5. v0.3 阶段未解决的遗留问题

### 5.1 LLM 输出不可控

**问题**：M-γ Step 4 调用 7 次 LLM 生成 7 件 YAML，LLM 偶发返回 schema 不兼容字段（如 WorldTree.branches 里有 LLM 魔改的 dict）。

**当前缓解**：
- `core/schemas/world_tree.py` 改 `extra="ignore"`（兼容多余字段）
- `services/onboarding.py` `branches` 兜底为 `[]`（不在 M-γ 写 Node）

**未来根治**：v0.4 设计更严格 prompt 模板 + 多次生成 pick 最佳 + 自检循环。

### 5.2 M-γ 验收慢（~5 min）

**问题**：8 次 LLM 调用串行（7 件 + 1 章），总计 ~5 min。

**当前缓解**：可接受（一次性验收）。

**未来优化**（v0.4）：
- 7 件合并成 1 次大 prompt 调用（预计 1-2 min）
- LLM 客户端加 `concurrent.futures` 并发（取决于服务端 QPS）

### 5.3 异常层级不完整

**问题**：`core/exceptions.py` 已有 5 类（ConfigError / ProjectError / LLMError / GenerationError / ProjectCorruptError），但 CLI 仍以 `print + return 1` 为主，未统一捕获。

**未来**：v0.4 用装饰器 `@handle_errors` + `try/except` 统一处理。

### 5.4 种子调度算法

**问题**：v0.2 时 planned_interval 偏长（10-20），20 章内 0/0 overdue 触发。**v0.3 阶段没改算法，沿用 v0.2**。

**未来**：v0.4 M1 调参（缩 planned_interval 或调整 sigmoid k）。

### 5.5 CLI 缺少 interactive 的回档前确认

**问题**：`cmd_rollback` 接受 ROLLBACK 文本确认，但**没显示被裁掉章节的列表**（"删 chapter-21/22/23" 是程序输出的，不是用户主动要求的预览）。

**未来**：v0.4 `cmd_rollback` 加 `--preview` 选项，先打印"如果回档会删什么"再确认。

### 5.6 M-δ `_delete_chapters_after_node` 启发式

**问题**：从 `node_id` 提取章节号（`re.search(r"chapter-(\d+)", node_id)`），如果 node_id 是预生成 `node-001`（无 chapter-NN），**返回 `[]` 不删任何章节**——这是合理默认，但**静默不删**可能误导用户。

**当前缓解**：rollback 结果返回 `warnings: []`，用户能看到"删 0 章节"。

**未来**：v0.4 加明确警告"目标 Node 不含章节号, 未删任何章节文件"。

---

## 6. v0.3 工程框架定型

### 6.1 强约束汇总（`conventions.md` 15 条）

| # | 规则 |
|---|------|
| 1-7 | 目录布局（顶层白名单 + 5 子包 + 7 件 Schema 位置 + LLM 独立）|
| 8-15 | 文档元信息 / commit 规范 / 单文件 ≤ 2000 行 / 路线图唯一 / 编号只在 design/ / *.private.json 不入仓 / 生成产物随项目保存 / `.tmp/` 调试不入仓 |

### 6.2 关键约定

- **5 子包分层**（core/services/adapters/cli/utils）—— 经典分层
- **LLM 客户端完全独立**（不引用 lunaris）
- **零重型依赖**（3 个第三方：pydantic / pyyaml / certifi）
- **CLI argparse 零依赖**
- **测试验收独立**（CLI 不跑验收，tests/ 独立）
- **.tmp/ 隐藏目录**调试临时文档（不入仓，避免与 `*.tmp` 后缀冲突）
- **路线图唯一原则**（`docs/roadmap/vX.Y-*.md` 同时只有 1 份 active）

### 6.3 Git 历史（v0.3 全部 commits）

```
5b6782f feat(s5): 骨架 0.4 - InterventionParser + RollbackManager
e2155b9 chore(structure): 调试临时文档改用 .tmp/ 隐藏目录（不入仓）
96baef7 feat(s3): 骨架 0.3 - OnboardingFlow 5 步启动链路
13767a2 chore(structure): generated-stories 合并入 projects/
a5f9701 refactor(structure): 5 子包分层 + LLM 独立 + tests/ + CLI
f7e0471 feat(s4): 骨架 0.2 - ChapterGenerator 接 LLM
cf559cb feat(s1,s2): 骨架 0.1 - ProjectManager + WorldTree + 7 件
53d655e chore(structure): 收尾
53653df chore(structure): docs 收口 + eval-notes 收编 + 工程内约束
c8d7ec2 docs(06): 骨架推进路线图 v1.0
362cd94 v0.2: 5 产品设计文档 + 评测代码 + 21 万字 demo
```

---

## 7. 后续路线（v0.4 建议）

> 本节是给欧尼酱的"我建议 v0.4 怎么走"，**不是新路线图**。新路线图应在 v0.4 启动时单独建。

### 7.1 优先级建议

| 优先级 | 里程碑 | 估时 | 关键产出 |
|--------|--------|------|---------|
| 🔴 P0 | **M-ε · M4 UI** | 5+ 天 | 启动链路 5 步 Web UI + 章节阅读器 + 干预输入面板 |
| 🟡 P1 | **算法调优 M1** | 1-2 天 | 缩 planned_interval + 跑 20/80 章验证 overdue 命中率 |
| 🟢 P2 | **pytest 升级** | 1 天 | tests/ 升级为 pytest + conftest.py 共享 fixtures |
| 🟢 P2 | **CLI 优化** | 1 天 | cmd_rollback --preview + cmd_generate --resume |
| 🔵 P3 | **远期架构 M3** | 3-5 天 | 300+ 章挑战（动态阶段摘要 + 向量检索）|
| 🔵 P3 | **多 agent 协作** | 5+ 天 | 主线旁白 agent + 人物 agent 协作 |

### 7.2 v0.4 启动建议

按欧尼酱 19:54 决定，**v0.4 第一个里程碑是 M-ε（M4 UI）**—— Web/IM 界面前端，需要：
- 新设计文档 `docs/design/05-ui-spec.md`
- 新的工程框架（前端部分）
- 决定技术栈：Streamlit / Textual / Web (FastAPI + Vue) / 直接 IM 集成

**M-ε 与 v0.3 骨架的边界**：
- v0.3 骨架 = CLI 入口 + 后端 orchestrators（不变）
- M-ε = 在 v0.3 之上加 UI 层（前后端）

### 7.3 路线图延续

v0.4 启动时应：
1. 改 `v0.3-product-skeleton.md` 状态 → ✅ 已收口
2. 移到 `docs/roadmap/archive/v0.3-product-skeleton.md`（按 §1.1 路线图唯一原则）
3. 新建 `docs/roadmap/v0.4-m4-ui.md`（或类似）作为 active 路线图
4. 写 `docs/roadmap/v0.3-completion.md`（本文件）作为收口报告

---

## Changelog

| 日期 | 变更 | 来源 |
|------|------|------|
| 2026-06-15 | v0.3 收口报告初版（M-α/M-β/M-γ/M-δ 4 里程碑汇总）| 蕾姆酱 🩵 |
