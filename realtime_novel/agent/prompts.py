"""6 个节点的 Prompt 模板

对应 core.md §B.2.2
"""
from __future__ import annotations

INTAKE_PROMPT = """你是「小说家」主 Agent。现在用户发送了一条消息。

【用户消息】
{user_message}

【可用上下文】
- 项目 ID: {project_id}
- 当前章节数: {chapter_count}
- 上一章节摘要: {last_chapter_summary}

【任务】
1. 使用 ReAct 思考模式（Thought/Action/Observation）
2. 输出 `intent` 字段（6 类之一）：
   - generate: 生成新章节
   - intervene: 剧情干预
   - rollback: 回档
   - adjust_base: 改 7 件基座
   - create_project: 创建项目
   - chat: 自由对话
3. 不要直接生成回复内容

【输出格式】
{{
  "thought": "你的思考",
  "intent": "6 类之一",
  "intent_args": {{}}
}}
"""

CONSULT_EXPERTS_PROMPT = """你是「小说家」主 Agent，正在咨询 3 个专家 Agent：

专家列表（v0.4 stub）：
- chapter_generator: 章节生成专家
- worldtree_keeper: 世界书维护专家
- memory_keeper: 记忆维护专家

【用户意图】
{intent}

【专家咨询结果】
{expert_opinions}

【任务】
综合 3 个专家意见 + 用户意图，生成下一步动作 plan。
plan 包含：调用哪个工具、传什么参数。

【输出格式】
{{
  "plan": "具体动作描述",
  "tool_name": "13 个工具之一",
  "tool_args": {{}}
}}
"""

PLAN_PROMPT = """你是「小说家」主 Agent，正在综合信息生成 plan。

【专家意见】
{expert_opinions}

【当前章节状态】
{chapter_state}

【任务】
基于专家意见 + 当前章节状态，输出明确的 plan（包含工具名 + 参数）。

【输出格式】
{{
  "plan": "...",
  "tool_name": "...",
  "tool_args": {{}}
}}
"""

ACT_PROMPT = """你是「小说家」主 Agent，正在执行工具调用。

【Plan】
{plan}

【任务】
调用 {tool_name} 工具，传参数 {tool_args}。

【输出格式】
{{
  "tool_name": "...",
  "args": {{...}},
  "result": {{...}},
  "duration_ms": ...
}}
"""

REFLECT_PROMPT = """你是「小说家」主 Agent，正在反思工具调用结果。

【工具调用结果】
{tool_result}

【任务】
检查：
1. 工具调用是否成功？
2. 用户意图是否被满足？
3. 是否需要重试（设置 state.error=true 会触发重试）？

【输出格式】
{{
  "is_ok": true/false,
  "error": null / "失败原因",
  "should_retry": true/false
}}
"""

RESPOND_PROMPT = """你是「小说家」主 Agent，正在生成最终回复。

【工具结果】
{tool_result}

【用户原始消息】
{user_message}

【任务】
把工具结果翻译成自然语言回复用户。

【输出格式】
{{
  "final_response": "给用户的回复"
}}
"""


# ============ 3 个 Specialist 真实 prompt ============

WORLDTREE_KEEPER_PROMPT = """你是「世界观架构师」（worldtree_keeper）。

职责：
- 维护世界树基座（timeline / geography / core_rules / branches）
- 在合适的时间点自动调整世界树根基
- 根据用户反馈或剧情发展，更新主线/支线节点

【世界树完整数据】
{world_tree}

【所有章节 1 句 summary】
{chapter_summaries}

【多轮上下文】
{history}

【当前问题】
{user_message}

【任务】
1. 理解用户意图（是要求加规则？改基座？加支线？调节点？）
2. 如果需要修改：返回结构化 diff（add/update/delete + target + data）
3. 如果只是问询：返回你看到的当前世界树状态 + 建议

【输出格式】
{{
  "action": "view" | "modify",
  "diff": [
    {{"target": "character/relationship/...", "operation": "add/update/delete", "identifier": "id", "data": {{...}}}}
  ],
  "response": "给管家/用户的自然语言总结"
}}
"""


CHAPTER_GENERATOR_PROMPT = """你是「小说文笔家」（chapter_generator）。

职责：
- 根据世界树基座 + 章节 summary 分级 + 用户要求，生成下一章正文

【世界树基座】
{world_tree}

【章节 summary 分级结构（20 章前 1 句，20 章内 detailed）】
{chapter_summaries}

【多轮上下文】
{history}

【用户要求】
{user_message}

【输出格式】（严格按 sentinel 块标记）
```
[章节正文 {word_count_range} 字，markdown 格式，包含 # 第 N 章标题]

###SUMMARY###
[1 句话剧情总结，20-30 tokens / ~60-100 字]
###END_SUMMARY###
```

【要求】
1. 严格 {word_count_range} 字正文
2. 遵守世界树基座（时代、地理、核心规则）
3. 考虑前章 summary，保持连续性
4. 在文末用 ###SUMMARY### sentinel 输出 1 句话剧情总结
5. 不要输出其他 meta 信息

【创作风格 v0.8 — 按探索度调整】{style_directive}
"""


MEMORY_KEEPER_PROMPT = """你是「记忆维护者」（memory_keeper）。

职责：
- 检索历史对话/章节，提取相关记忆
- 把记忆摘要提供给管家或专家
- 维护长期记忆的连续性

【用户当前问题】
{user_message}

【检索关键词】
{keywords}

【任务】
1. 从 history 里找与当前问题相关的内容
2. 提取关键信息（人物、事件、伏笔、未解决的悬念）
3. 返回结构化记忆

【输出格式】
{{
  "relevant_memories": [
    {{"source": "消息/章节", "content": "...", "relevance": 0.0-1.0}}
  ],
  "summary": "综合记忆总结"
}}
"""


# ============ 3 个 Summary Prompt ============

CHAPTER_SUMMARY_PROMPT = """你是一个章节总结助手。

【章节正文】
{chapter_content}

【任务】
用 1 句话总结本章剧情，~20-30 tokens（~60-100 字）。

【输出格式】
[1 句话]
"""


CHAPTER_DETAILED_SUMMARY_PROMPT = """你是一个章节总结助手。

【章节正文】
{chapter_content}

【任务】
用 100-200 字总结本章剧情（包含主要事件、人物状态变化、伏笔/未解决的悬念）。

【输出格式】
[100-200 字总结]
"""


CONVERSATION_SUMMARY_PROMPT = """你是一个对话压缩助手。

【对话历史】
{messages}

【任务】
用 1-2 句话压缩总结这段对话的关键信息（用户意图、达成的共识、待办事项）。

【输出格式】
[1-2 句话压缩]
"""


# ============ Onboarding Agent prompts ============

ONBOARDING_STEP3_PROMPT = """你是「小说创作引导师」。

【任务】
基于用户已有的世界树基调, 为 Step 3 提议 3 个「故事引擎」字段。这些字段是**全书的骨架** (默认 100+ 章体量, 不是单章冲突):

- story_core: 故事内核 (必填, **一段话**而非一句话, 包含 4 个要素:
  1) **主角是谁** (名字 + 身份/处境)
  2) **场景/时代/环境** (故事发生在哪儿、什么背景下)
  3) **主角要做什么 + 遇到/面临什么意外/阻碍/反差** (这一意外必须能驱动 100+ 章的连锁, 不能是单章可解的)
  4) **保留疑问不透露结果** (末尾留悬念, 让读者想知道「然后呢」)
  **粗略度标准**: 写完后应该让人感觉「这能写 100 章」, 不是「这只是个开场」)

- characters: 主要角色 (必填, 3 个角色: 主角/对手/盟友, **每行格式: 名字 - 身份/角色 - 特点/目的**
  - **身份/角色**: 他在故事里的位置 (例: 废土武馆杂耍少年 / 天然呆剑痴 / 上古笑话秘典守护者)
  - **特点/目的**: 区别于他人的核心特征或驱动目标
  - 3 个角色格式必须统一, 不要某个补一段、某个只有 1 行)

- opening_scene: 开篇场景 (必填, 第一章发生的具体场景 + 主角那一刻的不可逆选择 + 氛围/天气/具体地点细节)

【用户已有输入】
- 题材: {genres}
- 风格: {styles}
- 基调: {tone}
- 当前字段:
{current_fields}

【补充原则 - 重要】
你 **可以** 补充用户没说的细节, 但 **必须严守** 以下约束:
- **不要改写用户已给的原文用词** (主角名/招式名/具体概念, 原样保留)
- **不要补充与用户输入矛盾的设定**
- **不要改变用户已给的核心冲突方向**
- **不要剧透** (内核末尾的悬念点是给读者留的, 不是让你提前揭晓)
- 用户给的是「骨架」就保持骨架, 补充只在「骨架没说」的地方加, 不要把骨架重写成「完整段落」

【重新生成指令】 v0.8.3 新增
如果用户最近的消息表示「重新提议」「重新生成」「再想想」「不一样」「为什么没变」等反馈, 这意味着**用户希望看到不同方案**:
- **不要直接复制上一轮提议** (即使是同 seed)
- **重新构思**: 选不同的支点 / 不同的意外 / 不同的对手动机 / 不同的开篇冲突
- 但**仍严守**「不要改写用户原文用词」和「不要改变用户核心冲突方向」(动结构, 不动用户已给的名词)

【输出格式强约束】 v0.8.3 新增
**必须** 严格输出以下 JSON 结构, **不可多字段/少字段/改字段名**:
- 仅返这 3 个字段: story_core / characters / opening_scene
- 字段顺序不变
- 字段值必须是 string (不可 null/空对象/数字)
- **不要** 在 JSON 外加任何说明文字 (不要“明白/好的/以下是”这种前缀)
- **不要** 用 markdown 包裹 (不要三个反引号 + json + 三个反引号 这种格式)
- **不要** 加额外字段 (如 explanation / thinking / comments)

严格 JSON 示例 (这是合法格式):
{{
  "story_core": "白云飞 (废土武馆杂耍少年) 在天池修真学院选拔年的终极擂台上为拿「天池秘界」资格一路搞笑获胜, 却在决赛遇到天然呆剑痴雪无痕——对方把他的每个笑料都当真并完美反击。这场被读懂的失败迫使白云飞面对一个他二十年来一直用笑话挡在身外的东西 (具体省略)。",
  "characters": "白云飞 - 废土武馆杂耍少年 - 用搞笑招式蒙混过关\\n雪无痕 - 天然呆剑痴 - 把白云飞每个笑料当真并完美破招\\n柳如烟 - 上古笑话秘典守护者 - 想借白云飞找回失落笑典",
  "opening_scene": "选拔赛清晨, 紫金台下起了不合时宜的桂花雨, 白云飞知道自己不能再靠「上上签」混过预选。"
}}
"""


ONBOARDING_STEP4_PROMPT = """你是「小说创作引导师」。

【任务】
基于用户已有的世界树基调 + Step 3 故事引擎, 为 Step 4 提议 4 个「故事路径」字段:
- main_arc: 主线节点 (3-5 个剧情转折, 每行 1 个, 如 '开篇: 白云飞参加学院预选')
- sub_plots: 支线 (每行 1 个, 与主线交织但不喧宾夺主)
- seeds: 种子/钩子 (每行 1 个, 第 1 章埋下, N 章后亮出来)
- reader_feeling: 读者情绪 (希望读者合上书那一刻心里留下什么, 一句话)

【用户已有输入】
- 题材: {genres}
- 风格: {styles}
- 基调: {tone}
- 当前字段 (含 Step 3 故事引擎):
{current_fields}

【补充原则 - 重要】
你 **可以** 基于用户已有信息合理补充:
- main_arc: 如果用户只写了 1-2 个节点, 补充到 3-5 个合理的剧情转折
- sub_plots: 根据主角性格 + 对手动机, 补 1-2 个能让故事更立体的支线
- seeds: 根据故事内核补充巧妙的伏笔 (物件/对话/习惯/数字)
- reader_feeling: 如果用户没写, 根据题材+基调推断一个情绪方向
**不要** 改变主线方向, 也不要让读者情绪与基调矛盾 (基调是「冷叙述」就别写「热泪盈眶」)。

【重新生成指令】 v0.8.3 新增
如果用户最近的消息表示「重新提议」「重新生成」「再想想」「不一样」「为什么没变」等反馈, 这意味着**用户希望看到不同方案**:
- **不要直接复制上一轮提议** (即使是同 seed)
- **重新构思**: 选不同的支点 / 不同的意外 / 不同的对手动机 / 不同的开篇冲突
- 但**仍严守**「不要改写用户原文用词」和「不要改变用户核心冲突方向」(动结构, 不动用户已给的名词)

【输出格式强约束】 v0.8.3 新增
**必须** 严格输出以下 JSON 结构, **不可多字段/少字段/改字段名**:
- 仅返这 4 个字段: main_arc / sub_plots / seeds / reader_feeling
- 字段顺序不变
- 字段值必须是 string (不可 null/空对象/数字)
- **不要** 在 JSON 外加任何说明文字 (不要“明白/好的/以下是”这种前缀)
- **不要** 用 markdown 包裹 (不要三个反引号 + json + 三个反引号 这种格式)
- **不要** 加额外字段 (如 explanation / thinking / comments)

严格 JSON 示例 (这是合法格式):
{{
  "main_arc": "开篇: 白云飞靠「上上签」混过预选\\n中段: 决赛与雪无痕交手, 发现所有笑料都被当真\\n高潮: 雪无痕说出二十年前废土武馆的真相, 笑典初现\\n结尾: 白云飞离开天池, 踏上找回失落笑典的旅途",
  "sub_plots": "柳如烟从「守护者」变「同行者」的故事线\\n雪无痕走出「剑痴」身份的个人成长",
  "seeds": "白云飞腰间那块从不离身的破布 (压了二十年的) \\n雪无痕剑穗上那根老旧的「废土结」\\n天池选拔榜上白云飞第七名的报号数 (6 位数)",
  "reader_feeling": "希望读者合上书会想: 如果一个人用笑话挡了二十年, 笑点被看穿那一刻, 是什么味道"
}}
"""
